# -*- mode: snippet -*-
# name: macro_rules! name { (..) => (..); }
# key: macro
# group: macros declarative
# --
/// Declarative Macro
// $ident:type
// #[macro_export]
macro_rules! ${1:name} {
    ($2) => { $3 };
    $0
    // ($_:tt) => { panic!() };
}