## test.toml -*- mode: toml -*-
doot-version = "1.0"

[[locations]]

[[tasks.test]]
name = "pytest"
doc  = ["run project tests"]
setup = [] # TODO: use the right env
actions = [
        {do="shell!", args=["pytest"]},
]
