# -*- mode: snippet -*-
# name  : model_validator_before
# uuid  : model_validator_before
# key   : model_validator_before
# group : pydantic, decorators
# --
@model_validator(mode="before")
def $1 (cls, data:dict) -> dict:
$0
