# -*- mode: snippet -*-
# name  : propose
# uuid  : propose
# key   : propose
# group : rules operators
# --
sp{$1*propose
    (state <s> ^type state)
-->
    # (write |-- Proposing $1| (crlf))
    (<s> ^operator <o1> +)
    (<o1> ^name $1)
}