#  `(f-filename buffer-file-name)` -*- mode: gdscript -*-
class_name `(f-base buffer-file-name)`
extends SceneTree

"""
A Basic CLI Script for godot.

Run with:
godot --headless -s `(f-filename buffer-file-name)`
"""

func _init():
    # Constructor
    print("init")
    quit()
