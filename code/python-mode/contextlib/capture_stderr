# -*- mode: snippet -*-
# name  : capture_stderr
# uuid  : capture_stderr
# key   : capture_stderr
# group : IO with context redirection
# --
with contextlib.redirect_stderr(io.StringIO()) as stdout:
     $0
