# -*- mode: snippet -*-
# name  : find.to.array
# uuid  : find.to.array
# key   : find.to.array
# group :
# --
readarray -d '' ${1:arrayName} < <(find \
    "." \
    -name "*" \
    -type f \
    -maxdepth 0 \
    -print0 )