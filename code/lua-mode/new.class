# -*- mode: snippet -*-
# name  : new.class
# uuid  : new.class
# key   : new.class
# group :
# --
local ${1:ClassName} = {}

function $1:new (o)
  o = o or {}
  setmetatable(o, self)
  self.__index = self
  return o
end

$0

return $1