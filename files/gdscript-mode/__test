# `(f-filename (buffer-file-name))` -*- mode: gdscript -*-
class_name `(f-filename (buffer-file-name))`
extends GutTest
"""

"""

func _init(...args):
    pass

func test_sanity():
    gut.p("Testing Sanity", 2)
    assert_eq(1, 1)
